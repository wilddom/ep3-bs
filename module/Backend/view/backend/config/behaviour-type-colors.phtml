<?php

$this->setup(array(
    'title' => 'Edit booking type names and colors',
    'panel' => 'centered-panel large-sized',
    'back' => array(
        'Behaviour' => $this->url('backend/config/behaviour'),
    ),
));

?>

<div>
    <h1><?= $this->t('Edit booking type names and colors') ?></h1>

    <div class="separator-small"></div>

    <?= $this->backendInfo('i18n') ?>

    <div class="separator separator-line"></div>

    <div class="padded">
        <?php

        $form = $this->typeColorsForm;
        $form->setAttribute('method', 'post');
        $form->setAttribute('action', $this->url('backend/config/behaviour/type-colors'));
        $form->prepare();

        ?>

        <?= $this->form()->openTag($form) ?>

        <table class="default-table">
            <?= $this->formRowDefault($form, 'cf-type-colors') ?>

            <?= $this->formRowSubmit($form, 'cf-submit') ?>
        </table>

        <?= $this->form()->closeTag() ?>
    </div>
</div>
